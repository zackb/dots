blur = on
# Border and window
border_width            = 1
border_active           = 0x16b8f3cc
border_inactive         = 0x44444488
border_gradient         = 0xff2090ff

animation           = 150
workspace_animation = 0
flip                = 300

shadow           = on
shadow_color     = 0x00000055
shadow_blur      = 28

gaps             = 2
margin           = 0 0 0 0
rounding         = 6
opacity          = 0.95
scale            = auto
sensitivity      = 0.55
tap_to_click     = false

# Cursor
cursor          = BreezeX-RosePine-Linux
cursor_size     = 24

# ENV
env = QT_QPA_PLATFORM,wayland
env = QT_QPA_PLATFORMTHEME, qt6ct
env = QT_STYLE_OVERRIDE,kvantum
env = QT_QPA_PLATFORM,wayland
env = ELECTRON_OZONE_PLATFORM_HINT,auto

# Binds
bind = SUPER, R, exec, ghostty
bind = SUPER,       Q,      killactive
bind = SUPER SHIFT, Q,      exit
bind = SUPER,       H,      focusleft
bind = SUPER,       L,      focusright
bind = SUPER,       K,      focusup
bind = SUPER,       J,      focusdown
bind = SUPER,       F,      fullscreen
bind = SUPER,       V,      togglefloating
bind = SUPER,       P,      pin
bind = SUPER,       T,      togglesplit
bind = SUPER SHIFT CTRL, C, cleaning

# Fenriz desktop
bind = SUPER, Space,        exec, fenriz-desktop launcher
bind = SUPER SHIFT, L,      exec, fenriz-desktop lock
bind = SUPER, W,            exec, fenriz-desktop wallpaper
bind = SUPER, N,            exec, fenriz-desktop notifications
bind = SUPER, Tab,          exec, hyprwat --overview

# Workspaces
bind = SUPER,       1, workspace, 1
bind = SUPER,       2, workspace, 2
bind = SUPER,       3, workspace, 3
bind = SUPER,       4, workspace, 4
bind = SUPER,       5, workspace, 5
bind = SUPER,       6, workspace, 6
bind = SUPER,       7, workspace, 7
bind = SUPER,       8, workspace, 8
bind = SUPER,       9, workspace, 9
bind = SUPER,       0, workspace, 10
bind = SUPER SHIFT, 1, movetoworkspace, 1
bind = SUPER SHIFT, 2, movetoworkspace, 2
bind = SUPER SHIFT, 3, movetoworkspace, 3
bind = SUPER SHIFT, 4, movetoworkspace, 4
bind = SUPER SHIFT, 5, movetoworkspace, 5
bind = SUPER SHIFT, 6, movetoworkspace, 6
bind = SUPER SHIFT, 7, movetoworkspace, 7
bind = SUPER SHIFT, 8, movetoworkspace, 8
bind = SUPER SHIFT, 9, movetoworkspace, 9
bind = SUPER SHIFT, 0, movetoworkspace, 10

# Apps / shell
bind = SUPER,       E,      exec, ghostty -e yazi

# Screenshots
bind = ,      Print, exec, fenriz-bar shot region --edit --save
bind = SUPER, Print, exec, fenriz-bar shot region --edit --copy

# Media / brightness
binde = , XF86AudioRaiseVolume,  exec, fenriz-desktop volume +5
binde = , XF86AudioLowerVolume,  exec, fenriz-desktop volume -5
bind  = , XF86AudioMute,         exec, fenriz-desktop volume mute
bind  = , XF86AudioMicMute,      exec, fenriz-desktop volume micmute 

binde = , XF86MonBrightnessUp,   exec, fenriz-desktop brightness +5
binde = , XF86MonBrightnessDown, exec, fenriz-desktop brightness -5

bind = , XF86AudioNext,  exec, playerctl next
bind = , XF86AudioPause, exec, playerctl play-pause
bind = , XF86AudioPlay,  exec, playerctl play-pause
bind = , XF86AudioPrev,  exec, playerctl previous
bind = , XF86PowerOff,   exec, fenriz-bar open power

bind = SUPER SHIFT, M, flipmark
bind = SUPER SHIFT, P, flippair
bind = SUPER SHIFT, F, flip
bind = SUPER SHIFT, U, flipunpair

# Screen zoom / magnifier
zoom_mod         = ctrl
zoom_max         = 10.0
zoom_step        = 0.1

# Window rules
windowrule = name=pavucontrol, class=^(org\.pulseaudio\.pavucontrol)$, float=true, center=true
windowrule = name=satty,       class=^com\.gabm\.satty$,               float=true, center=true
windowrule = name=filechooser, class=^xdg-desktop-portal-gtk$,         float=true, center=true

# Exec
exec-once = fenriz-desktop
exec-once = fenriz-bar
